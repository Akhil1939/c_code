#include<conio.h>
 #include<stdio.h> #include<malloc> #include<stdlib.h> struct dlist { struct dlist *next,*prev; int data; }; struct dlist *start=NULL, *temp=NULL, *node=NULL,*temp1=NULL; void create() { char ch; temp = start=(struct dlist *)malloc(sizeof(struct dlist)); printf("\n Enter The Node Data : "); scanf("%d",&start->data); node->next=start; node->prev=temp; printf("\n You Wants To Create More Node (y/n) "); ch=getch(); node=start; while(ch=='y') { temp = node->next=(struct dlist *)malloc(sizeof(struct dlist)); node->next->prev=node; node=node->next; printf("\n Enter The New Node : "); scanf("%d",&node->data); node->next=start; start->prev=temp; printf("\n Wants To Create More Node (y/n) "); ch=getch(); } } void display_forward() { node=start; while(node->next!=start) { printf("\n %d", node->data); node=node->next; } printf("\n %d", node->data); printf("\n\t process done...."); } void display_backword() { node=start; while(node->next!=start) { node=node->next; } while(node->prev!=start) { printf("\n %d", node->data); node=node->prev; } printf("\n %d", start->data); } void insert_begin() { temp=(struct dlist *)malloc(sizeof(struct dlist)); printf("\n Enter New Node Data : "); scanf("%d", &temp->data); temp1=start->prev; start->prev = temp; temp->next=start; start=start->prev; temp->prev=temp1; } void insert_last() { node=start; while(node->next!=NULL) { node=node->next; } temp=(struct dlist *)malloc(sizeof(struct dlist)); printf("\n Enter New Node Data : "); scanf("%d", &temp->data); node->next=temp; temp->prev=node; temp->next=start; start->prev=temp; } int main() { int choice; while(1) { printf("\n ---------------------------------------------"); printf("\n 1. Create Doubly Circular Linked List"); printf("\n 2. Display Nodes In Forward Direction"); printf("\n 3. Display Nodes In Reverse Direction"); printf("\n 4. Insert Node At Begging"); printf("\n 5. Insert Node At Last"); printf("\n 11. Exit"); printf("\n ---------------------------------------------"); printf("\n Enter Your Choice : "); scanf("%d",&choice); switch(choice) { case 1: create(); getch(); break; case 2: display_forward(); getch(); break; case 3: display_backword(); getch(); break; case 4: insert_begin(); getch(); break; case 5: insert_last(); getch(); break; case 11: exit(0); default: printf("\n You Have Entered Invalid Choice"); getch(); } } }
